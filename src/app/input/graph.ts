import { InputGraph } from "../types";
import { inputEdges } from "./edges";
import { inputNodes } from "./nodes";

export const inputGraph: InputGraph = {
	nodes: inputNodes,
	edges: inputEdges,
	root: {
		name: "Outer Space -1"
	}
};
